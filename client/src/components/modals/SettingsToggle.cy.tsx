import React from 'react'
import {SettingsToggle} from './SettingsToggle'

describe('<SettingsToggle />', () => {
    it('renders', () => {
        // see: https://on.cypress.io/mounting-react
        // cy.mount(<SettingsToggle />)
    })
})